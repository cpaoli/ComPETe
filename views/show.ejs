<% include partials/header %>
<h1><%=pet.name%></h1>

<img src="<%= pet.image %>">

<p><%= pet.desc %></p>

<% pet.comments.forEach(function(e){ %>
    <strong><p><%= e.author %></p></strong> 
    <p><%= e.text %></p>
<% }); %>

<a href="/pets/<%= pet._id %>/comment/new"><button>Add a Comment</button></a>
<a href ="/pets">Go Back</a>
<% include partials/footer %>